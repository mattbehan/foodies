<h2><%= t "devise.invitations.new.header" %></h2>

<%= form_for @user, :url => "/users/invite", :html => {:method => :post} do |f| %>

	<% @user.class.invite_key_fields.each do |field| -%>
	  <p><%= f.label field %><br />
	  <%= f.text_field field %></p>

	<% end %>

	

	<button type="submit" formaction="/users/invite"> Invite user </button>
	<% if admin? %>
  	<button type="submit" formaction="/admins/invite"> Invite as reviewer </button>
	<% end %>
  </p>
<% end %>

<h1>Current users id: <% if current_user %> <%= current_user.id %> <% end %></h1>