<% unless user.admin? %>
  <div class="user-score btn">
    <strong>
      <% if user.reviewer? %>
        <%= user.reviewer_reputation %>
      <% end %>
    </strong>
  </div>
  <% if current_user && current_user == user %>
    <span class="hidden-xs"> (<%= user.english_reviewer_repuation %>)</span>
  <% end %>
<% end %>
