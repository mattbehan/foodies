<!-- pass in resource as a local -->
	<%= form_tag "/followings", :class => "follow-button" do %>
	  <%= hidden_field_tag "resource", "#{resource.class.to_s}" %>
	  <%= hidden_field_tag "resource_id", "#{resource.id}" %>
	  <%= submit_tag "Follow", :class => "btn btn-social" %>
	<% end %>
