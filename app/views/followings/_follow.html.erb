<!-- pass in resource as a local -->
	<%= form_tag "/followings", :class => "follow-button", remote: true do %>
	  <%= hidden_field_tag "resource", "#{@profile.class.to_s}" %>
	  <%= hidden_field_tag "resource_id", "#{@profile.id}" %>
	  <%= submit_tag "Follow", :class => "btn btn-primary btn-follow" %>
	<% end %>
