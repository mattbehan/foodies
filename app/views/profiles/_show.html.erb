<div>

	<p><%= profile.username %> </p>
	<p><%= profile.bio %></p>

	<% if profile.user.reviewer? %>
		<p><%= profile.full_name %></p>
		<p><%= profile.affiliation %></p>
	<% end %>

	<%if owner?(profile) %>
		<%= button_to edit_user_profile_path, method: "get" %>
	<% end %>

<div>