<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Foodie.com</title>
  <link href='https://fonts.googleapis.com/css?family=Lato:400,700,300,400italic' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <%= stylesheet_link_tag 'foodie' %>
  <%= javascript_include_tag "googlemaps" %>
  <%= stylesheet_link_tag 'googlemaps' %>
  <script type="text/javascript" src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  <%= javascript_include_tag "svg" %>
  <%= javascript_include_tag 'geolocation' %>

</head>

<body>
  <header>
    <!-- Mobile Nav Bar (Fixed to top) -->
    <div class="hidden-sm hidden-md hidden-lg">
    <nav class="navbar navbar-default navbar-fixed-top" id="main-nav">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-nav-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= image_tag("foodie.png", id: "navbar-logo") %></a>
          <%= form_tag(search_path, :method => "get", :class => "navbar-form navbar-left search-form") do %>
            <div class="input-append form-group">
              <%= text_field_tag :search, params[:search],
              class: "search-bar", placeholder: "Search for anything..." %>
              <!-- hidden fields -->
              <button class="btn search-button" type="submit"><i class="icon-search"></i></button>
            </div>
          <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="main-nav-collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to("#{current_user.username}", user_path(current_user)) %></li>
              <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
            <% else %>
                <li><%= link_to('Register', new_user_registration_path) %></li>
                <li><%= link_to('Login', new_user_session_path) %></li>
            <% end %>
          </ul>

          <!-- Search bar -->
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <div class="mobile-nav-padding"></div>
    </div><!-- end mobile navbar -->

    <!-- Desktop Navbar (Not fixed to top) -->
    <div class="hidden-xs">
    <nav class="navbar navbar-default" id="main-nav">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-nav-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><img src="/assets/foodie.png" alt="Foodie Brand" id="navbar-logo"></a>
          <%= form_tag(search_path, :method => "get", :class => "navbar-form navbar-left search-form") do %>
            <div class="input-append form-group">
              <%= text_field_tag :search, params[:search],
              class: "search-bar", placeholder: "Search for anything..." %>
              <!-- hidden fields -->
              <button class="btn search-button" type="submit"><i class="icon-search"></i></button>
            </div>
          <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="main-nav-collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to("#{current_user.username}", user_path(current_user)) %></li>
              <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
            <% else %>
              <li><%= link_to('Register', new_user_registration_path) %></li>
              <li><%= link_to('Login', new_user_session_path) %></li>
            <% end %>
          </ul>

          <!-- Search bar -->
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
  </div><!-- end desktop navbar -->

  </header>

  <main>
    <%= yield %>
  </main>

  <footer class="footer">
    <div class="container-fluid">
      <p>Site built and designed by <a href="https://github.com/chi-cicadas-2015/foodies">the Foodies Team</a>.</p>
    </div>
  </footer>

</body>
</html>
